import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    fixedComponent: {
      position: 'absolute',
      left: 0,
      right: 0,
      backgroundColor: 'lightgray',
      padding: 10,
      zIndex: 1,
    },
  });